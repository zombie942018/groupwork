```mermaid
graph TD
    subgraph "輸入層 (User Interfaces / Triggers)"
        ChatTrigger["💬 聊天室訊息<br>(Chat Message)"]
        GDriveTrigger["📂 Google Drive 檔案變動<br>(New/Update)"]
        FormTrigger["📝 行銷情報蒐集表單<br>(Form)"]
        ScheduleTrigger["⏰ 定期清理任務<br>(Schedule)"]
    end

    subgraph "AI 核心 (Brain - n8n AI Agent)"
        Agent["🧠 AI Agent<br>(Google Gemini)"]
        Memory["💾 Postgres 聊天記憶<br>(Long-term Memory)"]
        UserPref["👤 使用者偏好與<br>個性化設定"]
        Agent --- Memory
        Agent --- UserPref
    end

    subgraph "知識庫與 RAG (Knowledge Base)"
        subgraph "外部情報 (Market Knowledge)"
            SerpApi["🔍 SerpApi<br>(Google Search)"]
            JinaReader["📄 Jina Reader<br>(網頁抓取)"]
            ExtVector["🗄️ Supabase<br>外部向量資料表"]
        end

        subgraph "內部文件 (Internal Documents)"
            ExtFromFile["📑 檔案提取<br>(PDF/Excel/CSV)"]
            IntVector["🗄️ Supabase<br>內部向量資料表"]
            SQLData["📊 Postgres 結構化數據<br>(document_rows)"]
        end
    end

    subgraph "輸出與自動化 (Automation Output)"
        GDoc["📄 Google Docs<br>草稿生成"]
        Approval["📧 Gmail 審核信件<br>(Wait for Approval)"]
        PDFConv["🖨️ CloudConvert<br>(轉檔為 PDF)"]
        FinalShare["🔗 Google Drive 分享 /<br>Gmail 最終回覆"]
    end

    %% 連接關係
    ChatTrigger --> Agent
    GDriveTrigger --> ExtFromFile
    FormTrigger --> SerpApi
    
    SerpApi --> JinaReader --> ExtVector
    ExtFromFile --> IntVector
    ExtFromFile --> SQLData
    
    Agent -- "呼叫 RAG 工具" --> ExtVector
    Agent -- "呼叫內部檢索" --> IntVector
    Agent -- "執行 SQL 統計" --> SQLData
    
    Agent --> GDoc --> Approval
    Approval -- "核准後" --> PDFConv --> FinalShare
    ScheduleTrigger --> Agent
```

---
